# Android CI 构建密钥通用模板

适用于：App 需要第三方 API Key 等敏感配置，本地用 `local.properties`，CI 用 GitHub Secret 注入 `BuildConfig`，**密钥不进 Git**。

## 一、项目侧（每个 App 做一次）

### 1. 添加 manifest（提交到 Git）

```bash
cp ~/tools/scripts/ci-build-secrets.manifest.example signing/ci-build-secrets.manifest
# 编辑：每行 SECRET名 | local.properties键 | required|optional | 说明
```

示例（AiChatHub）：

```
DEEPSEEK_API_KEY|deepseek.api.key|required|DeepSeek API Key
DEEPSEEK_BASE_URL|deepseek.base.url|optional|DeepSeek API Base URL
DEEPSEEK_MODEL|deepseek.model|optional|DeepSeek Model
```

### 2. Gradle 读取（app/build.gradle.kts）

```kotlin
val localProperties = Properties().apply {
    rootProject.file("local.properties").takeIf { it.exists() }?.inputStream()?.use(::load)
}

fun configuredValue(name: String, localName: String = name): String =
    providers.gradleProperty(name).orNull
        ?: providers.environmentVariable(name).orNull
        ?: localProperties.getProperty(localName)
        ?: ""

android {
    defaultConfig {
        buildConfigField("String", "MY_API_KEY",
            "\"${configuredValue("MY_API_KEY", "my.api.key")}\"")
    }
    buildFeatures { buildConfig = true }
}
```

代码中使用 `BuildConfig.MY_API_KEY`。

### 3. 本地开发

`local.properties`（已在 .gitignore）：

```properties
my.api.key=sk-xxx
```

### 4. CI workflow（.github/workflows/release-notify.yml）

生成 env 片段：

```bash
cd ~/work/YourApp
~/tools/scripts/ci_render_workflow_env.sh signing/ci-build-secrets.manifest
```

Build 步骤示例：

```yaml
      - name: Build Release APK
        env:
          RELEASE_KEYSTORE_PATH: release.jks
          RELEASE_STORE_PASSWORD: ${{ secrets.RELEASE_STORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
          # ↓ 由 ci_render_workflow_env.sh 根据 manifest 生成
          MY_API_KEY: ${{ secrets.MY_API_KEY }}
        run: |
          chmod +x gradlew
          _tools/ci_check_build_secrets.sh signing/ci-build-secrets.manifest
          ./gradlew assembleRelease
```

`ci_check_build_secrets.sh` 会校验 manifest 中 `required` 项是否已通过 env 注入。

## 二、本机配置 GitHub Secret（每个 App / 每个密钥变更时）

```bash
# 从 local.properties 自动读取 + 交互补全
~/tools/scripts/setup-app-build-secrets.sh --project-dir ~/work/YourApp

# 或一次性通过环境变量
MY_API_KEY=sk-xxx ~/tools/scripts/setup-app-build-secrets.sh --project-dir ~/work/YourApp wzystal/YourApp
```

验证：

```bash
gh secret list --repo wzystal/YourApp
```

## 三、发布可用包

```bash
git push origin main          # 自动触发 Release workflow
# 或
gh workflow run "Release APK & DingTalk" --repo wzystal/YourApp
```

钉钉/蒲公英下载的 APK 已内置密钥，安装即可用。

## 四、与现有脚本关系

| 脚本 | 用途 |
|------|------|
| `setup-app-build-secrets.sh` | **通用**：按 manifest 写 GitHub Secret |
| `setup-deepseek-secret.sh` | 兼容入口，转调 `setup-app-build-secrets.sh` |
| `ci_check_build_secrets.sh` | CI 构建前校验 required 密钥 |
| `ci_render_workflow_env.sh` | 根据 manifest 生成 workflow env 片段 |
| `setup-github-secrets.sh` | Release 签名 RELEASE_* |
| `setup-shared-secrets.sh` | 蒲公英 / 钉钉（多仓库） |

一键 Release 签名 + 构建密钥（若存在 manifest）：

```bash
~/tools/scripts/setup-android-release.sh ~/work/YourApp
~/tools/scripts/setup-app-build-secrets.sh --project-dir ~/work/YourApp
```

## 五、安全说明

- GitHub Secret：加密存储，仅 Actions 可读，不出现在日志（勿 `echo` 密钥）。
- Release APK：**编译进 BuildConfig**，反编译可提取 → 仅适合个人/小范围分发。
- 公开大规模分发：应改为**后端代理**，App 不持有第三方 Key。

## 六、不需要 API Key 的项目

不添加 `signing/ci-build-secrets.manifest` 即可；`ci_check_build_secrets.sh` 找不到文件会跳过检查。
