# CI Release 构建密钥清单（提交到 Git，不含真实值）
#
# 格式（每行一条，竖线分隔）:
#   GITHUB_SECRET名 | local.properties键名 | required|optional | 说明
#
# - GITHUB_SECRET名: 与 workflow env / secrets.XXX 一致，CI 构建时注入为环境变量
# - local.properties键名: 本地开发用，setup-app-build-secrets.sh 可从此读取
# - required: CI 未配置则构建失败；optional: 可缺省
#
# 接入步骤见 ~/tools/scripts/app-build-secrets.README

DEEPSEEK_API_KEY|deepseek.api.key|required|DeepSeek API Key
DEEPSEEK_BASE_URL|deepseek.base.url|optional|DeepSeek API Base URL
DEEPSEEK_MODEL|deepseek.model|optional|DeepSeek Model
